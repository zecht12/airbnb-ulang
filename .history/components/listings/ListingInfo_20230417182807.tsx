'use client'

import { SafeUser } from '@/app/types'
import React from 'react'

interface ListingInfoProps{
    user:SafeUser
}

const ListingInfo:React = () => {
    return (
        <div>ListingInfo</div>
    )
}

export default ListingInfo